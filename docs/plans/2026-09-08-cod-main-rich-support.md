# COD Main Rich Support Implementation Plan

**Goal:** Only COD country main images use one dominant claim, 3–5 distinct source-backed supporting benefits when available, and 1–2 direct visual proof areas. Keep details, hooks, JP25, Amazon, Rakuten, models and nodes unchanged.

**Architecture:** Store a bounded, traceable supporting-benefit inventory on eligible main pages. Collect candidates from explicit current-product sources and original-reference product observations, never previous generated images or planner filler. Preserve primary source identities for coverage. The same inventory and budget must reach normalization, director requests, final compilation, modules and review; no neighboring-benefit blanket rule may contradict the main-only exception.

**Tech Stack:** Pure Python selection helpers, existing Python/JavaScript panel and isolated unittest mocks.

## Work

1. Add pure support selection/normalization/instruction helpers and negative tests for duplicates, title repetition, filler and insufficient evidence.
2. Bind supporting source records to eligible COD main pages; retain five labels through JSON roundtrips, using source text rather than fabricated labels.
3. Align source isolation, rich density, module geometry, main renderer and director messages with the approved main-image exception. Preserve text-free and explicitly minimal requests.
4. Pass source inventory and exact label budget to review; allow approved supporting claims while still rejecting unsupported claims and unrelated additions.
5. Update only the COD country plan/cache/asset version. Run all regressions and scope checks.
6. Wait for current local work to finish before activation. Preserve files, settings, images and logs; no paid generation, push or VPS deployment.

The 3–5 target does not authorize inventing missing benefits, numbers, reviews or certifications. The existing detail one-point contract remains intact.

## Completion and verification — 2026-09-08

- Added a pure, provenance-preserving support selector, main-only integration, five-label normalization, shared director/render/review rules and source-backed evidence validation. The primary headline remains separate from the support inventory. Fewer actual sources retain their actual count; text-free and explicitly minimal requests retain their requested treatment.
- Main image source records survive JSON roundtrips and are included once in the final prompt source-data block. Image observations are rebuilt from matching server analysis rather than browser labels, and rendering checks the uploaded product reference against the saved analysis reference hashes.
- COD country plan version is `cod-country-v24-rich-main-support`; COD detail remains `cod-detail-v16-source-backed` and JP25 remains `director-v33-online-photography-plan`. Country-only dynamic UI hints now expose the support budget. Script asset revision is `20260908-cod-rich-main-v9`.
- Final targeted regression run: **388 tests passed** across 13 COD/JP25/frontend modules, using temporary data and mocked HTTP/model calls. `node --check sku_board/static/app.js` and `git diff --check` passed. Existing line-ending conversion warnings remain advisory.
- At activation, the persisted queue had zero queued/running image jobs and port 8793 had no established request connections. Only the verified Python panel process was stopped. The existing `SOSOVE-SKU-Board-8793` supervisor restarted it, and `/api/health` returned `ok: true`.
- Verified the locally served v9 JavaScript matches the current file byte-for-byte. Shared environment and saved director-settings hashes remained unchanged across activation. Logs were preserved; no node/model configuration edit, Git push, VPS deployment or live image-generation charge was performed.
- Visual output quality still needs a fresh user generation run; existing completed images are unchanged. Refresh the panel and replan to use the new country main-image contract.
